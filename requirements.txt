aiogram>=3.20,<4
httpx>=0.28,<1
--extra-index-url https://download.pytorch.org/whl/cu121
# RTX 20xx / NVIDIA 535 supports CUDA 12.1 wheels; do not let pip select CUDA 13.
torch==2.5.1+cu121
# Newer ONNX Runtime CUDA wheels require CUDA 13 and make FASHN pose detection
# silently fall back to CPU on the CUDA 12.1 server environment.
onnxruntime-gpu==1.20.2
diffusers>=0.35
transformers>=4.51
accelerate>=1.4
sentencepiece>=0.2
pillow>=10
python-dotenv>=1
loguru>=0.7
pydantic-settings>=2.7
pytest>=8
# Main local VTON backend (Apache-2.0); install pulls the inference package,
# while scripts/setup_server.sh downloads its weights separately.
fashn-vton @ git+https://github.com/fashn-AI/fashn-vton-1.5.git
